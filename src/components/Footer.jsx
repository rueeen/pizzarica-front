import Logo from'./Logo';import{whatsappUrl,dias}from'../data/site';import'../styles/Footer.css';
export default function Footer({site}){const open=site.horarios.filter(h=>h.abre);return <footer className="footer"><div className="container footer__grid"><div className="footer__logo"><Logo/></div><div><h2>Conversemos</h2><a href={`https://instagram.com/${site.instagram}`} target="_blank" rel="noopener noreferrer">Instagram: @{site.instagram}</a><a href={whatsappUrl()} target="_blank" rel="noopener noreferrer">Pedir por WhatsApp</a></div><div><h2>Horario</h2><p>{open.length?`${dias[open[0].dia]} a ${dias[open.at(-1).dia]}, desde las ${open[0].abre}`:'Horario por confirmar'}</p><p>© {new Date().getFullYear()} {site.nombre}</p></div></div></footer>}
