import{dias}from'../data/site';import'../styles/Ubicacion.css';
const todayIn=(tz)=>{const short=new Intl.DateTimeFormat('en-US',{timeZone:tz,weekday:'short'}).format(new Date());return['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].indexOf(short)};
export default function Ubicacion({site}){const today=todayIn(site.zonaHoraria);const maps=`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(site.direccion)}`;return <section id="ubicacion" className="section location" aria-labelledby="location-title"><div className="container"><h2 id="location-title">Dónde estamos</h2><div className="location__grid"><div><h3>{site.direccion}</h3><p>{site.referencia}</p><a className="button button--coral" href={maps} target="_blank" rel="noopener noreferrer">Cómo llegar</a><dl className="schedule">{site.horarios.map(h=><div key={h.dia} className={today===h.dia?'is-today':''}><dt>{dias[h.dia]}{today===h.dia&&<span>Hoy</span>}</dt><dd>{h.abre?`${h.abre}–${h.cierra}`:'Cerrado'}</dd></div>)}</dl></div><div className="map">{site.mapaEmbedUrl?<iframe src={site.mapaEmbedUrl} title="Mapa de ubicación de PizzArica" loading="lazy"/>:<><span aria-hidden="true">⌖</span><strong>{site.direccion}</strong><small>El mapa aparecerá cuando confirmemos la ubicación.</small></>}</div></div></div></section>}
