import{useEffect,useRef,useState}from'react';import{whatsappUrl}from'../data/site';import'../styles/ComoPedir.css';
export default function ComoPedir({site}){const ref=useRef(),[visible,setVisible]=useState(false);useEffect(()=>{const node=ref.current;const observer=new IntersectionObserver(([e])=>{if(e.isIntersecting){setVisible(true);observer.unobserve(node)}},{threshold:.4});observer.observe(node);return()=>observer.disconnect()},[]);return <section ref={ref} id="como-pedir" className={`section order ${visible?'is-visible':''}`} aria-labelledby="order-title"><div className="order__panel container"><h2 id="order-title">Cómo pedir</h2><div className="steps"><svg viewBox="0 0 100 2" preserveAspectRatio="none" aria-hidden="true"><path pathLength="100" d="M0 1h100"/></svg>{site.pasos.map((paso,i)=><div className="step" style={{'--delay':`${i*400}ms`}} key={paso}><span>{i+1}</span><p>{paso}</p></div>)}</div><a className="button button--whatsapp" href={whatsappUrl()} target="_blank" rel="noopener noreferrer">Pedir por WhatsApp</a></div></section>}
